# App Config — [Your App Name]

Copy this file to `app-config.md` and fill in your real values. `app-config.md` is gitignored.

**URL:** https://your-app.example.com

## Test credentials

- Profile: **TestUser**
- Password: **yourpassword**

## Login flow

1. Describe step one of the login flow (e.g. landing page shows a profile picker)
2. Describe step two (e.g. click profile avatar → password input appears)
3. Describe step three (e.g. enter password → click Sign in → lands on home screen)
